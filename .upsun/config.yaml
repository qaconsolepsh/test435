# Upsun configuration, generated using AI at: 2026-04-14T11:15:37+01:00
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app:
    type: nodejs:24
    build:
      flavor: none
    hooks:
      build: |
        set -ex
    web:
      commands:
        start: node index.js
      locations:
        /:
          passthru: true

routes:
  https://{default}/:
    type: upstream
    upstream: app:http