# Upsun configuration, generated using AI at: 2026-05-04T15:48:01.882Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app:
    type: nodejs:24
    build:
      flavor: none
    hooks:
      build: |
        set -ex
        npm install
    web:
      commands:
        start: node index.js

routes:
  https://{default}/:
    type: upstream
    upstream: app:http